package routes

import (
	"{{ .Module }}/internal/interfaces/http/handlers"
	"github.com/gin-gonic/gin"
)

func Register(r *gin.Engine, ping *handlers.PingHandler) {
	v1 := r.Group("/api/v1")
	{
		v1.GET("/ping", ping.Ping)
		v1.GET("/pong", ping.Pong)
	}
}
